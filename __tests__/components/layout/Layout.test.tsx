import '@testing-library/jest-dom'

describe('Layout', () => {
  //stubs
  it('renders container', () => {})
  it('renders contents inside container', () => {})
  it('renders navbar when authenticated', () => {})
  it('renders sidemenu when authenticated', () => {})
})
